// Register the root component with Expo after you run `npx expo install`.
// Kept as a scaffold so the monorepo documents the mobile surface.
console.log(
  "StatusPass mobile scaffold. Use Expo to register App.tsx and POST tokens to /api/push-tokens.",
);
